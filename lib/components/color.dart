import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF0066FF); // The primary color you mentioned
  static const Color backgroundColor = Colors.white;   // Background color for the app
  static const Color textColor = Colors.black;         // Default text color
  static const Color secondaryColor = Colors.blue;     // Secondary color for buttons, highlights, etc.
}
